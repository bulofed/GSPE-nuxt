const { plugin } = require("mongoose");

module.exports = {
    darkMode: 'class',
    content: [],
    theme: {
        plugins: [],
        fontFamily: {
            'sans': 'Lato, sans-serif',
        }
    }
}